<h2>商品詳細</h2>
<div>
  <div>
  <%= attachment_image_tag @item, :image, format: "jpeg", fallback: "no_image.jpg", size: "100x100" %>
  </div>
  <div>
    <div>商品名</div>
    <%= @item.name %>
    
    <div>商品説明</div>
    <%= @item.explanation %>
    
    <div>ジャンル</div>
    <%= @item.genre.name %>
    
    <div>税込価格(税抜価格)</div>
      <div><%= @item.add_tax_price %>円</div>
      <div>(<%= @item.price %>円)</div>
      
    <div>販売ステータス</div>
      <div>
        <% if @item.sold_out? %>
          <span>販売中</span>
        <% else %>
          <span>販売停止中</span>
        <% end %>
      </div>
    <%= link_to "編集する", edit_item_path(@item),class: "btn-outline-dark" %>
  </div>
</div>