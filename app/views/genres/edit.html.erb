<h2>ジャンル編集</h2>
<table>
    <tr>
    <%= form_with model:@genre, local:true do |f| %>
      <td>ジャンル名</td>
      <td><%= f.text_field :name %></td>
      <td><%= f.submit "変更を保存" %></td>
    <% end %>
    </tr>
   
</table>
